{
  "name": "evidence-forge",
  "private": true,
  "scripts": {
    "dev": "powershell -NoProfile -ExecutionPolicy Bypass -File .\\run-dev.ps1",
    "dev:raw": "concurrently -k -n BACKEND,FRONTEND -c green,cyan \".venv\\Scripts\\python.exe -m uvicorn app.main:app --app-dir backend --port 8330\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix frontend run build",
    "test": "pytest backend/tests"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
